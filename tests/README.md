# refla unit tests

## Setup
From the root directory of this repository:

```bash
$ pip install -r tests/requirements
```

## Running tests
From the root directory of this repository:

```
$ py.test tests
```